[project]
name = "posetrak"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "opencv-python>=4.13.0.92",
    "pandas>=3.0.0",
    "rerun-sdk>=0.29.2",
    "toml>=0.10.2",
]

[dependency-groups]
dev = [
    "marimo>=0.19.7",
    "pytest>=8.0",
    "pytest-cov>=5.0",
]
